# Video Folder Duration

A small CLI tool to calculate the total duration of all video files in a folder using ffprobe.

## Requirements
- Python 3
- ffmpeg (for ffprobe)

## Usage
```bash
python main.py
